{
  "name": "@curator/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "curator": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@curator/scheduler": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": { "vitest": "^2.1.8" }
}
